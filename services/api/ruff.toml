[lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # line length handled by formatter

[lint.per-file-ignores]
"tests/*" = ["E402"]  # allow imports after os.environ in conftest
"migrations/*" = ["E402", "F401"]
